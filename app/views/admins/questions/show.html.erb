
<%= @question.content %>
<% @question_images.each do |image| %>
   <%if !image.image_url.nil? %>
     <%= image_tag image.image_url, style: "max-width: 200px; max-height: 200px"  %>
    <%end %>
<% end %>
<%= link_to  "Back", new_admins_question_path %>